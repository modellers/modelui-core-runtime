import Layout from './Layout'

describe('LayoutRender', () => {
  it('is exported', () => {
    expect(Layout.LayoutRender).toBeTruthy()
  })
})

describe('Layouter', () => {
  it('is exported', () => {
    expect(Layout.Layouter).toBeTruthy()
  })
})
