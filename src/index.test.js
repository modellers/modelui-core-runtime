import { layout, structs, util } from '.'

describe('structs', () => {
  it('is truthy', () => {
    expect(structs).toBeTruthy()
  })
})

describe('layout', () => {
  it('is truthy', () => {
    expect(layout).toBeTruthy()
  })
})

describe('util', () => {
  it('is truthy', () => {
    expect(util).toBeTruthy()
  })
})
