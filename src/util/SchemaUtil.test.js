import { generateIdentifier, getSchemaGeneratedData } from './SchemaUtil'

describe('generateIdentifier', () => {
  it('is exported', () => {
    expect(generateIdentifier).toBeTruthy()
  })
})

describe('getSchemaGeneratedData', () => {
  it('is exported', () => {
    expect(getSchemaGeneratedData).toBeTruthy()
  })
})
