import { compareDeep, mergeDeep } from './ObjUtil'

describe('compareDeep', () => {
  it('is exported', () => {
    expect(compareDeep).toBeTruthy()
  })
})

describe('mergeDeep', () => {
  it('is exported', () => {
    expect(mergeDeep).toBeTruthy()
  })
})
